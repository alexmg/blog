export type Site = {
  title: string;
  description: string;
  author: string;
  locale: string;
  featuredPostCount: number;
  postsPerPage: number;
};

export type Social = {
  title: string;
  icon: string;
  url: string;
};
