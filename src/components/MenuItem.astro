---
import { Icon } from "astro-icon/components";

interface Props {
  href: string;
  icon: string;
  text: string;
  target?: string;
}

const { href, icon, text, target = "_self" } = Astro.props;
---

<a
  href={href}
  target={target}
  class="font-heading link-highlight flex items-center font-bold text-gray-700 dark:text-gray-300"
>
  <Icon name={icon} class="mr-3 text-xl" />
  {text}
</a>
